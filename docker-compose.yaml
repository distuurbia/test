version: "3.9"

services:
  postgres-positions:
    image: postgres
    environment:
      POSTGRES_DB: "testNotify"
      POSTGRES_USER: "postgresql"
      POSTGRES_PASSWORD: "postgresql"
    ports:
      - 5439:5432
